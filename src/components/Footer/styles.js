import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  footer: {
    textAlign: 'center',
    padding: '10px 0',
    color: '#143050',
    fontSize: '15px',
    fontWeight: '600',
  }
}))